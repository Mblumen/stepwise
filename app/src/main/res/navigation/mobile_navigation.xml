<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_active_tracks">

    <fragment
        android:id="@+id/nav_active_tracks"
        android:name="de.hd.fitbittracks.ui.activetracks.ActiveTracksFragment"
        android:label="@string/menu_transform"
        tools:layout="@layout/fragment_active_tracks" />

    <fragment
        android:id="@+id/nav_slideshow"
        android:name="de.hd.fitbittracks.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_slideshow"
        tools:layout="@layout/fragment_slideshow" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="de.hd.fitbittracks.ui.settings.SettingsFragment"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>