<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FitbitTracks" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/dark_gray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="bottomNavigationStyle">@style/Widget.FitbitTracks.BottomNavigationView</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
    </style>

   <!-- <style name="Theme.FitbitTracks.NoActionBar" parent="Theme.FitbitTracks">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>-->
    <style name="CustomToolbarStyle" parent="Widget.Material3.Toolbar">
        <item name="titleCentered">true</item>
        <item name="titleTextAppearance">@style/TextAppearance.Material3.TitleLarge</item>
    </style>

    <style name="Theme.FitbitTracks.AppBarOverlay" parent="ThemeOverlay.Material3.Toolbar.Surface" />

    <style name="Theme.FitbitTracks.PopupOverlay" parent="ThemeOverlay.Material3.Light" />

    <style name="Widget.FitbitTracks.BottomNavigationView" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="backgroundTint">@color/white</item>
        <item name="itemIconTint">@drawable/bottom_nav_item_selector_light</item>
        <item name="itemTextColor">@drawable/bottom_nav_item_selector_light</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>

</resources>